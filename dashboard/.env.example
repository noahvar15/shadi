# Server-side rewrite destination (not exposed to browser)
API_URL=http://localhost:8000

# Set to "true" to intercept API calls with MSW mock handlers (no backend needed)
# NEXT_PUBLIC_MOCK_API=true

// MSW request handlers for offline / test mock mode.
// This file requires `bun add msw` to activate.
// Once installed, import these handlers in src/providers.tsx and set up
// the MSW browser worker before the QueryClientProvider tree.
export const handlers: unknown[] = []
